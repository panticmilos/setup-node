on: 
  push:
    branches:
      - "main"
  workflow_dispatch:

jobs:
  node-latest:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        node-version: [current, latest, node, "16.14.2"]
    steps:
      - name: Retrieve version before install
        run: |
          version=$(echo $(node --version))
          echo "::set-output name=NODE_VERSION::$version"
        id: version
        shell: bash
      - uses: actions/checkout@v3
      - name: Setup Node
        uses: panticmilos/setup-node@main
        with:
          node-version: ${{ matrix.node-version }}
      - name: Compare versions
        if: steps.version.outputs.NODE_VERSION ==  $(node --version)
        run: |
          exit 1